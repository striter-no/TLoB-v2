#share-screen {
    align: center middle;
}

#share-cont {
    padding: 1 1;
    width: 40;
    height: 30;
    background: $surface;
}

#share-label {
    content-align: center top;
    color: $primary;
    width: 1fr;
    text-style: italic bold;
    margin: 0 0 2 0;
}

.value {
    text-wrap: nowrap;
    width: 1fr;
    color: $accent;
}